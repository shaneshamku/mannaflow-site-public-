{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.ts|*.tsx) out=$(npx tsc --noEmit 2>&1) || { echo \"$out\" >&2; exit 2; } ;; esac",
            "timeout": 90,
            "statusMessage": "Type-checking..."
          }
        ]
      }
    ]
  }
}
